# Copie ce fichier en ".env.local" puis colle tes 2 valeurs Supabase.
# (Supabase → Project Settings → API)
# Tant que ces 2 lignes sont vides, le site marche en mode local (données dans le navigateur).

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
